import {Route} from "./route";

export interface RoutesPrivate {
    home: Route;
    // users: Route;
    // locals: Route;
    // book: Route;
    // reviews: Route;
    // gamificacion: Route;
    // search: Route;
}